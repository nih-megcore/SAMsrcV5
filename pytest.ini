[pytest]
markers =
    unit: fast tests that do not require external scientific data
    integration: CTF and AFNI end-to-end tests
    slow: opt-in full-volume AFNI and CTF beamformer tests
testpaths = test
